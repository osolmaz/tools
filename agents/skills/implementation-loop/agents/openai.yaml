interface:
  display_name: "Implementation Loop"
  short_description: "Finish a plan and ready its PR"
  default_prompt: "Use $implementation-loop to implement the approved plan end-to-end, test it, run `codex review --base <branch>` until no P0/P1 findings remain, clear valid PR feedback, and confirm CI/CD is green without merging."
