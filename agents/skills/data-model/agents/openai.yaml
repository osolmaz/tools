interface:
  display_name: "Data Model"
  short_description: "Use Schemator for structured data models"
  default_prompt: "Use $data-model to route this schema or data-model task through Schemator and its bundled schemator skill."
