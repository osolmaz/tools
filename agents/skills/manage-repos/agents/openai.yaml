interface:
  display_name: "Manage Repos"
  short_description: "Set GitHub repo metadata and defaults"
  default_prompt: "Use $manage-repos to set repository descriptions, apply GitHub defaults, and configure strict merge-review rules."
