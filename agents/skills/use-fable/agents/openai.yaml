interface:
  display_name: "Use Fable"
  short_description: "Explicitly requested, costly Fable calls via local Claude"
  default_prompt: "Use Claude Fable only because the human explicitly requested it. Fable is very expensive, so make the fewest narrowly scoped calls needed. Invoke it through ACPX's local Claude adapter, never Cursor, with the explicit Fable model and an appropriately long timeout, then verify its result locally."
